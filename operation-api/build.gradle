tasks.named('bootJar') {
    enabled = true
}

tasks.named('jar') {
    enabled = false
}

dependencies {
    implementation project(path: ':operation-business')

    implementation 'org.springframework.boot:spring-boot-starter-web'
}
